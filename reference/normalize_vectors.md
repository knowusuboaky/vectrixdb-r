# Normalize vectors for cosine similarity

Normalize vectors for cosine similarity

## Usage

``` r
normalize_vectors(vectors)
```

## Arguments

- vectors:

  Matrix of vectors (rows are vectors)

## Value

Normalized matrix
